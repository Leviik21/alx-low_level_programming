#include "main.h"
#include <stdio.h>


/**
 * main - function that prints uppercase
 *
 * Return: Always 0.
 */
int main(void)
{
	int _isupper(int c);

	if(ch >= 'A' && ch <= 'Z')
	return (1);

	return (0);
}
